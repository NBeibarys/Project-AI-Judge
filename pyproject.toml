[project]
name = "ai-fellowship-agent"
version = "0.1.0"
description = "Multimodal ADK workflow for evidence-grounded fellowship review"
requires-python = ">=3.12,<3.13"
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }
